# ── Django Core ───────────────────────────────────────────────────────────────
Django>=5.2,<6.0
djangorestframework>=3.15
django-cors-headers>=4.3
django-filter>=24.2

# ── Autenticação JWT ──────────────────────────────────────────────────────────
djangorestframework-simplejwt>=5.3

# ── Autenticação Social (Google OAuth) + Recuperação de Senha ─────────────────
django-allauth[socialaccount]>=0.56.0

# ── Banco de Dados (PostgreSQL) ───────────────────────────────────────────────
# Em produção (Cloud Run): psycopg2 compila contra libpq do SO (mais eficiente que binary)
# Em desenvolvimento local: psycopg2-binary é suficiente (sem libpq instalado)
psycopg2-binary>=2.9
dj-database-url>=2.1      # lê DATABASE_URL do Supabase / Railway / DigitalOcean

# ── Variáveis de Ambiente (OBRIGATÓRIO — usado em settings.py) ────────────────
python-decouple>=3.8      # lê .env com config() — NÃO remover

# ── Segurança ─────────────────────────────────────────────────────────────────
django-axes>=6.4          # rate limiting / bloqueio por tentativas de login
whitenoise>=6.6           # serve arquivos estáticos em produção sem nginx extra

# ── Servidor de Produção ──────────────────────────────────────────────────────
gunicorn>=21.2

# ── Monitoramento de Erros (recomendado em produção) ─────────────────────────
sentry-sdk[django]>=2.0

# ── Upload e Processamento de Imagens ────────────────────────────────────────
Pillow>=10.3

# ── SMS via Twilio (descomente quando for ativar verificação por celular) ──────
# twilio>=9.0

# ── Armazenamento Google Cloud Storage (imagens em produção) ─────────────────
django-storages[gcs]>=1.14
google-cloud-storage>=2.10

# ── Tarefas Assíncronas (ativar quando precisar de filas) ─────────────────────
# celery>=5.4
# redis>=5.0
# django-celery-results>=2.5

# ── Documentação automática da API ────────────────────────────────────────────
# drf-spectacular>=0.27

# ── Utilitários de desenvolvimento (remover em requirements-prod.txt) ─────────
django-extensions>=3.2
